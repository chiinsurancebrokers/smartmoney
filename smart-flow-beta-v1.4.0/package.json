{
  "name": "smart-flow-intelligence-beta",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"node --watch server/index.js\"",
    "build": "vite build",
    "start": "node server/index.js",
    "smoke:assets": "node scripts/smoke-assets.mjs",
    "smoke:bigmove": "node scripts/smoke-bigmove.mjs",
    "smoke:report": "node scripts/smoke-report.mjs",
    "smoke": "npm run smoke:assets && npm run smoke:bigmove && npm run smoke:report",
    "check": "npm run smoke && npm run build"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "concurrently": "^9.1.2",
    "vite": "^6.3.5",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
